version: "3.8"

services:
  cliente:
    build:
      context: ./grpc/cliente
    depends_on:
      - servidor
    networks:
      - grpc-network

  servidor:
    build:
      context: ./grpc/servidor
    ports:
      - "50051:50051"
    networks:
      - grpc-network

networks:
  grpc-network:
